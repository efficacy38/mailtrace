# Mailtrace environment variables
# Copy this file to .env (alongside config.yaml) and fill in your values.
# These are used as fallback when passwords are not set in config.yaml.

MAILTRACE_OPENSEARCH_PASSWORD=
MAILTRACE_SSH_PASSWORD=
MAILTRACE_SUDO_PASSWORD=
